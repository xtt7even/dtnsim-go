package painter

import (
	simmap "sandbox/sandbox/map"
	"sandbox/sandbox/peer"
)

func Draw(m *simmap.Map, peers *[]peer.Peer) {
	//TODO
	// for y := 0; y < m.Size; y++ {
	// 	for x := 0; x < m.Size; x++ {
	// 	}
	// }
}